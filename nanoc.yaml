# base_url are needed for XML sitemap
# domain: woolie.co.uk
base_url: https://www.woolie.co.uk

enable_output_diff: true

# Think these are the default anyway, need to check before removing.
string_pattern_type: glob
identifier_type: full

# a list of file extensions that nanoc will consider to be textual rather than
# binary. if an item with an extension not in this list is found,  the file
# will be considered as binary.
text_extensions: [ 'adoc', 'asciidoc', 'atom', 'coffee', 'css', 'erb', 'haml', 'handlebars', 'hb', 'htm', 'html', 'js', 'less', 'markdown', 'md', 'ms', 'mustache', 'php', 'rb', 'rdoc', 'sass', 'scss', 'slim', 'tex', 'txt', 'xhtml', 'xml' ]

prune:
  auto_prune: true

data_sources:
  - type: filesystem
    encoding: utf-8
